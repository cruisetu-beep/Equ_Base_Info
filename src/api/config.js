// ── api/config.js ──────────────────────────────────────────────────
// MOCK 开关：true = 使用静态数据兜底，false = 请求真实后端
// 后端就绪后只需改此处，所有 api/*.js 自动切换

export const USE_MOCK = true

export const BASE_URL = '/api'
